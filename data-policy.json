{
  "plugin": "tools",
  "_comment": "Display-only manifest: /cleanup renders these dirs as an inventory of what the tools plugin owns. Nothing here is machine-enforced — cleanup/archive actions are decided interactively by the /cleanup command, not driven by this file.",
  "dirs": [
    {
      "path": "sessions-meta.json",
      "description": "Session status registry (per-project)",
      "cleanup": "managed"
    },
    {
      "path": "plans/",
      "description": "Plan files (active); archived to plans/archive/ on cleanup",
      "cleanup": "archive"
    },
    {
      "path": "plans/archive/",
      "description": "Archived plans",
      "cleanup": "manual"
    }
  ]
}
